apply plugin: 'com.android.application'

android {
  compileSdkVersion 22
  buildToolsVersion "22.0.1"

  defaultConfig {
    applicationId "com.sqisland.nfc.hunt"
    minSdkVersion 10
    targetSdkVersion 22
    versionCode 1
    versionName "1.0.0"
  }
}